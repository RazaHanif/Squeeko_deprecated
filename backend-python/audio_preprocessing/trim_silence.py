import os

# Trims leading and trailing silence + long pauces within audio
# Returns file path to trimmed audio
def apply(input_path: str) -> str:
    
    # TODO: Use ffmpeg to detect and trim silence at start & end
    
    # TODO: Use VAD or volume threshold to trim extended Internal silence
    
    # TODO: Save to a /temp or /trimmed folder & Return path to the cleaned audio file
        
    pass
